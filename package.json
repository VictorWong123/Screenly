{
  "name": "chronica",
  "version": "1.0.0",
  "description": "Manual timer + CRUD time tracker",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "cd apps/website && npm run dev",
    "build": "cd apps/website && npm run build",
    "preview": "cd apps/website && npm run preview"
  }
}